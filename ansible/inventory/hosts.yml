---
all:
  hosts:
    phobos.lab.home.morey.tech:
    rancher.lab.home.morey.tech:
  children:
    docker:
      children:
        kind:
        sandbox_nodes:
    kind:
      hosts:
        kind-personal.lab.home.morey.tech:
        kind-akuity.lab.home.morey.tech:
    sandbox_nodes:
      children:
        sandbox_master_nodes:
        sandbox_worker_nodes:
    sandbox_master_nodes:
      hosts:
        sandbox-master-[0:2].lab.home.morey.tech:
    sandbox_worker_nodes:
      hosts:
        sandbox-worker-[0:2].lab.home.morey.tech:
    rubrik:
      hosts:
        rubrik-a.maas.home.morey.tech:
          ansible_user: ubuntu
        rubrik-b.maas.home.morey.tech:
          ansible_user: ubuntu
        rubrik-c.maas.home.morey.tech:
          ansible_user: ubuntu
        rubrik-d.maas.home.morey.tech:
          ansible_user: ubuntu
  vars:
    ansible_user: root