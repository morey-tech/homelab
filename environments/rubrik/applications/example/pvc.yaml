apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guestbook-vol
  namespace: example
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
