apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-services
spec:
  generators:
  - git:
      repoURL: https://github.com/morey-tech/homelab.git
      revision: 5-new-gitops-structure
      directories:
      - path: environments/kind-personal/cluster-services/*
  template:
    metadata:
      name: '{{path[3]}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/morey-tech/homelab.git
        targetRevision: 5-new-gitops-structure
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: 'argocd'
      syncPolicy:
        automated:
          allowEmpty: true
          prune: true
          selfHeal: true
        syncOptions:
        - allowEmpty=true
