# Example LocalVolume CR for Local Storage Operator
# Uncomment and configure based on your storage devices
#
# apiVersion: local.storage.openshift.io/v1
# kind: LocalVolume
# metadata:
#   name: local-disks
#   namespace: openshift-storage-system
#   annotations:
#     # CRD might not be installed yet during initial Argo CD sync.
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
# spec:
#   nodeSelector:
#     nodeSelectorTerms:
#       - matchExpressions:
#           - key: cluster.ocs.openshift.io/openshift-storage
#             operator: In
#             values:
#               - ""
#   storageClassDevices:
#     - storageClassName: local-sc
#       volumeMode: Filesystem
#       fsType: xfs
#       devicePaths:
#         - /dev/vdb
