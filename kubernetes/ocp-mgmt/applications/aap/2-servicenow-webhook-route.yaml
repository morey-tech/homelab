kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: aap-external
spec:
  host: aap.ocp-mgmt.morey.tech
  path: /
  to:
    kind: Service
    name: aap
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
  wildcardPolicy: None